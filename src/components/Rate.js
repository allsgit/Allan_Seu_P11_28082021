import React from "react";
import "../style/_ficheLogement.scss";
import Data from "./Data";
import FullRateStar from "../images/rate_star_full.png";
import EmptyRateStar from "../images/rate_star_empty.png";

/* class Rate extends React.Component {
  render() {
    const starzArray = [...Array(parseInt(this.props.rating))];
    const starzArray2 = [...Array(5)];

    return starzArray2.map((starts) => {
      return <img className="rating" src={EmptyRateStar}></img>;
    });
  }
}

export default Rate;
 */